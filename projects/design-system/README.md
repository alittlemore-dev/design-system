# @alittlemoron/design-system

Application-independent Angular UI and Markdown building blocks shared by `my-site` and
`personal-workspace`.

The package is currently a library scaffold. Its public UI, Markdown, editor, styles, and testing
entry points will be added as their contracts are implemented.
